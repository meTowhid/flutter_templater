const projectTemplate = 'project_template';

class Constant {
  Constant._();

  static const String helpFlag = 'help';
  static const String fileOption = 'file';
  static const String defaultConfigFile = 'pubspec.yaml';
  static const templateConfig = 'template_config';
}
